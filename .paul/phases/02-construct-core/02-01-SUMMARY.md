---
phase: 02-construct-core
plan: 01
subsystem: infra
tags: [ydotool, grim, tesseract, uinput, wayland, playwright, uv, systemd]

requires:
  - phase: 01-design
    provides: pyproject.toml with all deps declared, project scaffold
provides:
  - CachyOS dev environment fully configured for HyprAgent development
  - All system tools installed and callable without sudo
  - uinput kernel module loaded and persistent on boot
  - ydotoold daemon running with socket at /run/user/1000/.ydotool_socket
  - Python venv with all deps installed (uv sync complete)
  - Playwright Chromium installed
  - install-deps.sh for future one-command setup
affects: [02-02-screenshot, 02-03-mouse-keyboard, 02-04-ocr, 02-05-backend]

tech-stack:
  added: [ydotool, grim, slurp, wl-clipboard, tesseract, tesseract-data-eng, playwright-chromium]
  patterns: [YDOTOOL_SOCKET env var must be set in all tool modules using ydotool]

key-files:
  created:
    - install-deps.sh
    - .venv/ (generated by uv sync)
  modified: []

key-decisions:
  - "ydotool.service unit name is ydotool.service (not ydotoold.service) on this CachyOS install"
  - "YDOTOOL_SOCKET=/run/user/1000/.ydotool_socket must be set explicitly — ydotool does not auto-detect"
  - "ydotoold was pre-running from a prior session; systemd service showed start-limit-hit but daemon functional"

patterns-established:
  - "All tools that invoke ydotool must set YDOTOOL_SOCKET env var before calling subprocess"

duration: ~45min (including two session breaks due to sudo TTY constraint)
started: 2026-04-02T00:00:00Z
completed: 2026-04-02T09:00:00Z
---

# Phase 2 Plan 01: M0 Environment Setup Summary

**CachyOS dev environment fully configured: ydotool installed, uinput loaded, ydotoold running at /run/user/1000/.ydotool_socket, Python venv complete, Playwright Chromium installed.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~45 min (2 sessions) |
| Started | 2026-04-02 |
| Completed | 2026-04-02 |
| Tasks | 2/2 complete + checkpoint approved |
| Files created | 1 (install-deps.sh) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: All system tools callable without sudo | Pass | grim, ydotool, wl-clipboard, tesseract all callable |
| AC-2: uinput loaded + ydotoold running | Pass | uinput in lsmod; ydotoold PID 76591 active; socket at /run/user/1000/.ydotool_socket |
| AC-3: Python venv with all deps | Pass | mcp, anthropic, playwright, pytesseract, PIL, pydantic, yaml, rich all import OK |
| AC-4: Playwright Chromium installed | Pass | `from playwright.sync_api import sync_playwright` succeeds |
| AC-5: install-deps.sh documents setup | Pass | pacman, modprobe, usermod, systemctl, uv sync, playwright install all present |

## Accomplishments

- All system dependencies installed; grim, slurp, wl-clipboard, tesseract were already present — only ydotool was missing
- uinput kernel module loaded and configured to persist via /etc/modules-load.d/uinput.conf
- ydotool communication verified: `YDOTOOL_SOCKET=/run/user/1000/.ydotool_socket ydotool mousemove --absolute -x 100 -y 100` exits 0
- User already in `input` group — M2 group prerequisite satisfied

## Files Created

| File | Purpose |
|------|---------|
| `install-deps.sh` | Documented one-command setup script for future installs |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Service name is `ydotool.service` not `ydotoold.service` | Discovered during setup — CachyOS package uses different unit name | Updated install-deps.sh accordingly |
| YDOTOOL_SOCKET env var required | ydotool does not auto-discover socket path | All M2 tool code (mouse.py, keyboard.py) must set this env var before subprocess calls |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 1 | Negligible |
| Deferred | 1 | Logged to STATE.md |

**ydotool.service unit name** — Plan referenced `ydotoold.service`; actual unit on CachyOS is `ydotool.service`. Auto-fixed in install-deps.sh.

**ydotoold start-limit-hit** — systemd service shows "failed" because daemon was already running when service attempted to start. Socket functional; ydotool communication verified. Deferred: fix service unit (Type=forking or socket activation) — low priority, does not block M1–M9.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| sudo requires TTY — could not run from Claude Code | Created install-deps.sh; user ran commands manually in terminal |
| ydotoold service showed "failed" state | Verified daemon running via pgrep; tested socket communication directly; confirmed functional |

## Next Phase Readiness

**Ready:**
- M1 (Screenshot): `grim` callable, `Pillow` installed — `tools/screenshot.py` stubs ready to implement
- M2 (Mouse/Keyboard): `ydotool` callable with known socket path — `tools/mouse.py` and `tools/keyboard.py` ready
- M3 (OCR): `tesseract` + `pytesseract` installed — `tools/ocr.py` ready
- M4 (Backend): `anthropic` package installed — `agent/backends/claude.py` ready

**Concerns:**
- YDOTOOL_SOCKET must be set as env var in M2 implementation — easy but must not be forgotten
- ydotoold service unit cleanup (start-limit-hit) is cosmetic but should be addressed before M13 release

**Blockers:**
- None — M1 (Screenshot) can begin immediately

---
*Phase: 02-construct-core, Plan: 01*
*Completed: 2026-04-02*
